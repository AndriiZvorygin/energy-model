import type { ChartSeries } from './chartTypes'

export function SeriesControls({ series, visible, focused, onToggle, onFocus }: { series: ChartSeries[]; visible: Set<string>; focused: string | null; onToggle: (key: string) => void; onFocus: (key: string | null) => void }) {
  return <fieldset><legend className="mb-2 text-xs font-semibold uppercase text-stone-500">Series</legend><div className="flex flex-wrap gap-2">{series.map((item) => <div key={item.key} className={`flex items-center border ${visible.has(item.key) ? 'border-stone-300 dark:border-stone-700' : 'border-stone-200 opacity-60 dark:border-stone-800'}`}><label className="flex cursor-pointer items-center gap-2 px-2.5 py-2 text-xs"><input type="checkbox" checked={visible.has(item.key)} onChange={() => onToggle(item.key)} /><span className="h-2.5 w-2.5" style={{ backgroundColor: item.color ?? '#0f766e' }} /><span>{item.label}</span></label>{visible.has(item.key) && <button type="button" className={`border-l px-2 py-2 text-xs ${focused === item.key ? 'bg-stone-200 font-semibold dark:bg-stone-700' : 'text-stone-500'}`} onClick={() => onFocus(focused === item.key ? null : item.key)} aria-label={`${focused === item.key ? 'Stop emphasizing' : 'Emphasize'} ${item.label}`}>Focus</button>}</div>)}</div></fieldset>
}
