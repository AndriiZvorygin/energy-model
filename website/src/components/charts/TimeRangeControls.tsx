export function TimeRangeControls({ range, from, to, onChange, onReset }: { range: string; from?: string; to?: string; onChange: (range: string, from?: string, to?: string) => void; onReset: () => void }) {
  return <fieldset><legend className="mb-2 text-xs font-semibold uppercase text-stone-500">Time range</legend><div className="flex flex-wrap items-end gap-2"><div className="inline-flex border border-stone-300 dark:border-stone-700">{['5y', '10y', '20y', 'all'].map((item) => <button key={item} type="button" onClick={() => onChange(item)} className={`px-3 py-2 text-xs font-semibold uppercase ${range === item ? 'bg-petroleum text-white' : ''}`}>{item}</button>)}</div><label className="text-xs text-stone-500">From<input type="date" value={from ?? ''} onChange={(event) => onChange('custom', event.target.value || undefined, to)} className="ml-2 h-9 border border-stone-300 bg-white px-2 dark:border-stone-700 dark:bg-[#18201d]" /></label><label className="text-xs text-stone-500">To<input type="date" value={to ?? ''} onChange={(event) => onChange('custom', from, event.target.value || undefined)} className="ml-2 h-9 border border-stone-300 bg-white px-2 dark:border-stone-700 dark:bg-[#18201d]" /></label><button type="button" onClick={onReset} className="h-9 border border-stone-300 px-3 text-xs font-semibold dark:border-stone-700">Reset</button></div></fieldset>
}
